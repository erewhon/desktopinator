knit together local and remote displays into one big virtual display a client can connect to

snapshot state of windows, positions, etc. save as json? sqlite database? includes displays (virtual or physical), resolutions, and where windows are. allow point in time rollback of positions. can move windows to virtual display, then will restore back to previous layout?

include remote monitors? for example, screen casting? or it can be a receiver? so you can extend on to another computer?

single static binary
