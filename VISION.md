A grab bag of ideas and features:

- Rust
- extensible / plugins. But ideally sandboxed by default
- tiling compositor for Wayland
- can work remotely
- VNC, RDP, and Sunshine servers
- can use locally; can support changing resolution on for remote desktop (will lock screen?). Fully virtualizable. So local can be one monitor, but remote can be higher resolution two monitors? Or vice versa?
- Tailscale aware? or built-in?
- Can use accelerators _if_ available
